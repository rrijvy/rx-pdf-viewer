import { FC } from "react";
import { PdfPageViewerProps } from "../../types";

const ZoomControl: FC<PdfPageViewerProps> = (props: PdfPageViewerProps) => {
  return <div></div>;
};

export default ZoomControl;
